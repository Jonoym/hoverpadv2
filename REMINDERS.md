## Features
- Different Status should be made and colour (Indigo) for when the session is Idle but agents are running

## Issues
- Agent logs do not update consistently - always all at once
- Changes to the notes causes it to flash on the control panel

## Changes
- Remove the Note and Status label from the Session and Note windows. For the session window, replace it with a dot to the left of the window name - this should be the colour of the status

## Status Bar Changes
- Show the number of notes, sessions (active, idle, done and runnign agents statuses) at the top
- make it clear when an agent is done


need to fix naming and window updates